# Rspamd logging configuration

# Log to stdout for Docker container observability
type = "console";

# Info level for production (change to "debug" for troubleshooting)
level = "info";

# Show module names in log output
log_usec = true;
